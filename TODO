Ana:
* History OK option do a log file to have previous processus history
* Redirections
* Signals
* Builtins:
	- export
	- unset
	- env

Stefan:
* Shell arguments
* Handle parsing errors with redirections (< < for example)

* ^C in heredoc
* Try to export vars like SHLVL, PWD, OLDPWD, HOME
* Ensure output is written at the right place
* Norminette!!
* Handle exit status on parsing error
* Variable substitution in heredoc

To remember ; 
Echo $$ > 12312 (not required in the subject)
